# util
http://nodejs.org/api/util.html

# util.inherits(sonstructor, superConstructor)
# sub仅仅继承在super原型中定义的属性，并且原型中的属性不会被console.log作为对象的属性输出。# utilinherits.js

# util.inspect(object, [showHidden],[depth],[colors])
# util.inspect是将任意对象转换为字符串的方法，通常用于调试的错误输出。
# util.inspect不会简单的将对象转换为字符串，即使对象定义了toString方法。# utilinspcet.js

